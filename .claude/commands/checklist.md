# チェックリスト管理

タスクのチェックリストを表示・更新します。

## 使用方法

引数: $ARGUMENTS（タスクID）

## 実行手順

1. docs/TASKS.mdから指定タスク（#$ARGUMENTS）のセクションを特定
2. チェックリスト部分（`- [ ]` / `- [x]`）を抽出して表示
3. 更新依頼があれば該当項目を `- [x]` に変更

## 表示形式

```
タスク #$ARGUMENTS のチェックリスト:
- [ ] 項目1
- [x] 項目2（完了）
進捗: 1/2 完了
```
